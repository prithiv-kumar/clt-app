const express = require('express');
const router = express.Router();

const registerController = require('../controllers/register/register'); // Assuming controllers folder is in the same parent directory as routes

// POST /register route
// router.post('/register', registerController.signup);

// module.exports = router;
